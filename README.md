
  # Prototype creation

  This is a code bundle for Prototype creation. The original project is available at https://www.figma.com/design/nQOJIcx2wnfEar01sMsBmr/Prototype-creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  